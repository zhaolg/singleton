<view class="swiper-container">
  <swiper class="swiper_box" autoplay="{{autoplay}}" circular="{{circular}}" interval="{{interval}}" duration="{{duration}}" previous-margin="{{windowWidth/5}}rpx" next-margin="{{windowWidth/5}}rpx">
    <block wx:for="{{personDetail.photo}}" wx:key="id">
      <swiper-item class="swiper-box">
        <image src="{{item.picurl}}" mode="widthFix" />
      </swiper-item>
    </block>
  </swiper>
</view>
<view style="height:5rpx;background: #eee;;width:100%;"></view>
<!-- 个人基本信息：姓名 年龄 星座 身高  体重  学历  大学名称 -->
<doc-page title="{{userInfo.nickName}}" without-padding>
  <zan-panel title='基本信息'>
    <view class="capsule-demo">
      <zan-cell>
        <zan-capsule type="danger" leftText="现居" rightText="{{personDetail.basicInfo.curr_province}}" />
        <zan-capsule type="danger" leftText="籍贯" rightText="{{personDetail.basicInfo.native_province}}" />
        <zan-capsule type="danger" leftText="{{personDetail.basicInfo.edu}}" rightText="{{personDetail.basicInfo.college}}" />
        <zan-tag type="danger" plain>{{personDetail.basicInfo.gender}}</zan-tag>
        <zan-tag type="danger" plain>{{personDetail.basicInfo.age}}</zan-tag>
      </zan-cell>
    </view>
  </zan-panel>

  <zan-panel title='我的个人介绍' class="self-introduction">
    <view class="capsule-demo">
      <view style="padding: 15px;">{{personDetail.basicInfo.self_introduction}}</view>
    </view>
  </zan-panel>

  <zan-panel title='我的标签'>
    <zan-cell>
      <block wx:for="{{personDetail.basicInfo.myTag}}" wx:key="item">
        <zan-tag type="{{item.type}}" plain>{{item.tagName}}</zan-tag>
      </block>
    </zan-cell>
  </zan-panel>

  <zan-panel title=' ' style="margin-top:20px;" class="{{showView?'modifyByMy_show':'modifyByMy_hide'}}">
    <zan-button size="large" type="danger" bindtap="modifySelfInfo" >编辑我的资料</zan-button>
  </zan-panel>
</doc-page>